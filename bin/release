#!/usr/bin/env bash
# bin/release <build-dir>

mkdir -p "$2/frontend"
$(dirname $0)/compile-nodejs "$1/frontend" "$2/frontend" $3

mkdir -p "$2/backend"
$(dirname $0)/compile-ruby "$1/backend" "$2/backend" $3

echo "build dir"
echo $0

echo "pwd"
echo $(pwd)